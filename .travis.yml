sudo: required
dist: trusty

language: sql

addons:
  postgresql: "9.5"
  apt:
    packages:
    - postgis
    - postgresql-9.5-postgis-2.2

script:
  - echo "select version()" | psql template1
  - make check RUNTESTFLAGS=-v
 
